// the regularjs has no context property like react,
// so it has been no way to make the store as the context,
// and you should pass the store explicitly as a 
// attribute of the container or by the connect method with the 3th argument.